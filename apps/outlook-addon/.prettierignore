# Generated by webpack. CI checks formatting before any build, so this only
# matters locally — but a config change that emitted minified HTML would
# otherwise fail the format gate on output nobody wrote.
dist
